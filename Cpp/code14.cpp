#include<iostream>
using namespace std;

// Inline function example:
// inline int mul(int a, int b)
// {
//     return a*b;
// }
// Static function example:
// int call(int a)
// {
//     static int b = 0;
//     b = b + 1;
//     return a+b;
// }
// Default argument example:
// float expensivedays(int todayprice, float factor = 1)
// {
//     return todayprice*factor;
// }
// // Constant argument example:
// int strleg(const int *p)
// {
//     //
// }
int main()
{
    // For Inline function:
    // int a = 5, b = 10;
    // cout<<"5 x 10 is "<<mul(a, b)<<endl;
    // cout<<"5 x 10 is "<<mul(a, b)<<endl;
    // cout<<"5 x 10 is "<<mul(a, b)<<endl;
    // cout<<"5 x 10 is "<<mul(a, b)<<endl;
    // cout<<"5 x 10 is "<<mul(a, b)<<endl;
    // cout<<"5 x 10 is "<<mul(a, b)<<endl;
    // cout<<"5 x 10 is "<<mul(a, b)<<endl;
    // cout<<"5 x 10 is "<<mul(a, b)<<endl;
    // cout<<"5 x 10 is "<<mul(a, b)<<endl;
    // cout<<"5 x 10 is "<<mul(a, b)<<endl;
    
    // For Static function:
    // int a;
    // cout<<"Enter value of 'a' ";
    // cin>>a;
    // cout<<endl;
    // cout<<"'a' dances with shadow 'b' and makes: "<<call(a)<<endl;
    // cout<<"'a' dances with shadow 'b' and makes: "<<call(a)<<endl;
    // cout<<"'a' dances with shadow 'b' and makes: "<<call(a)<<endl;
    // cout<<"'a' dances with shadow 'b' and makes: "<<call(a)<<endl;
    // cout<<"'a' dances with shadow 'b' and makes: "<<call(a)<<endl;
    // cout<<"'a' dances with shadow 'b' and makes: "<<call(a)<<endl;

    // For Default argument:
    // int a;
    // cout<<"Enter todays price: ";
    // cin>>a;

    // cout<<"This item will stay "<<expensivedays(a)<<" after 1 year if everything is well"<<endl;
    // cout<<"This item will become "<<expensivedays(a, 1.05)<<" after 1 year"<<endl;

    // For Constant argument:
    // 
    
    return 0;
}